---
import CardCertification from "../components/cards/CardCertification.astro";
import LinkedinIcon from "../components/icons/linkedin-icon.astro";
import AcademyCyberIcon from "../components/icons/academy_cyber-icon.astro";
import CiscoBadgeIcon from "../components/icons/badge_creddly-icon.astro";
import HMIcon from "../components/icons/badge_HM-icon.astro";
import LpiIcon from "../components/icons/lpi-icon.astro";
import {
    BADGE_CISCO_URL,
    LINKEDIN_URL,
    PUCE_URL,
    ACAD_CIBER_URL,
    ACAD_HM_BADGE_URL,
    ACAD_CIBER_BADGE_URL,
    LPI01_CERT_URL,
} from "../constants/urls.ts";
import {
    cardsCertHackerMentor,
    cardsCertAcademyMexico,
    cardsCertCisco,
    cardsCertLPI,
} from "../data/skills.json";
---

<section id="certifications">
    <span class="title">Certificaciones 🔖</span>
    <div class="certifications">
        <div class="certification">
            <div class="cards">
                {
                    cardsCertHackerMentor.map(
                        ({ name, imagePath, alt, link }) => (
                            <CardCertification
                                name={name}
                                imagePath={imagePath}
                                alt={alt}
                                link={link}
                            />
                        ),
                    )
                }
                <div class="links">
                    <a class="btn-icon" target="_blank" href={ACAD_HM_BADGE_URL}
                        >Click para ver👉 Certificaciones<HMIcon />
                    </a>
                </div>
            </div>
            <div class="cards">
                {
                    cardsCertCisco.map(({ name, imagePath, alt, link }) => (
                        <CardCertification
                            name={name}
                            imagePath={imagePath}
                            alt={alt}
                            link={link}
                        />
                    ))
                }
                <div class="links">
                    <a class="btn-icon" target="_blank" href={BADGE_CISCO_URL}
                        >Click para ver👉 Certificaciones<CiscoBadgeIcon />
                    </a>
                </div>
            </div>
            <div class="cards">
                {
                    cardsCertLPI.map(({ name, imagePath, alt, link }) => (
                        <CardCertification
                            name={name}
                            imagePath={imagePath}
                            alt={alt}
                            link={link}
                        />
                    ))
                }
                <div class="links">
                    <a class="btn-icon" target="_blank" href={LPI01_CERT_URL}
                        >Click para ver👉 Certificaciones<LpiIcon />
                    </a>
                </div>
            </div>
            <div class="cards">
                {
                    cardsCertAcademyMexico.map(
                        ({ name, imagePath, alt, link }) => (
                            <CardCertification
                                name={name}
                                imagePath={imagePath}
                                alt={alt}
                                link={link}
                            />
                        ),
                    )
                }
                <div class="links">
                    <a
                        class="btn-icon"
                        target="_blank"
                        href={ACAD_CIBER_BADGE_URL}
                        >Click para ver👉 Certificaciones<AcademyCyberIcon />
                    </a>
                </div>
            </div>
        </div>
    </div>
    <div class="more">
        <a
            href="https://www.linkedin.com/in/deed2812/"
            target="_blank"
            class="btn"
        >
            Ver más en LinkedIn <LinkedinIcon /></a
        >
    </div>
</section>

<style>
    section#certifications {
        display: flex;
        flex-direction: column;
        justify-content: center;
        position: relative;
        width: 100%;
    }

    .certifications {
        display: flex;
        flex-direction: column;
        gap: 1.6rem;
        width: 100%;
    }

    .certifications .certification {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .subtitle {
        font-size: 28px;
        font-weight: bold;
        color: var(--font-color);
    }
    .tretitle {
        font-size: 20px;
        font-weight: bold;
        color: var(--font-color);
    }

    .cards {
        display: flex;
        grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
        grid-auto-rows: 1fr;
        grid-auto-flow: dense;
        grid-gap: 0.8rem;
    }
    .links {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: flex-start;
    }
    .links {
        flex-direction: row;
        flex-wrap: wrap;
        gap: 0.5rem;
    }
    .btn {
        color: var(--bg-color-4);
        background-color: var(--bg-color-button-3);
    }
    .links .btn-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 12px 16px;
        border-radius: 0.5rem;
        background-color: var(--bg-color-button-4);
        color: var(--bg-color);
        font-size: 1.2rem;
        font-weight: 600;
        margin-right: 1rem;
        transition: all 0.3s ease;
        gap: 0.5rem;
    }
    .more {
        display: flex;
        align-items: center;
        justify-content: center;
        margin-top: 3rem;
    }
    @media (max-width: 900px) {
        section#certifications {
            justify-content: flex-start;
            align-items: center;
        }
        section#certifications h1.title {
            align-self: flex-start;
        }
    }

    @media (max-width: 480px) {
        .subtitle {
            font-size: 22px;
        }
    }
</style>
